# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 안내 문서입니다.

## 저장소 개요

1970년대 서울 소극장 연구를 위한 데이터 저장소입니다. 코드 저장소가 아니라 **데이터 저장소**이며, xlsx/owl/html/png 등 연구 자료 파일로만 구성되어 있습니다. 파일 구조와 용도는 [README.md](README.md)를 참고하세요.

## 작업 시 주의사항

- **대용량 파일**: `oral_history_변환용.txt`(195MB)는 GitHub 100MB 제한 때문에 `.gitignore`에 포함되어 저장소에서 추적되지 않습니다. 새로 추가/복원하지 마세요. `1970년대 소극장_대상자원.xlsx`(78MB)는 추적되지만 GitHub 권장 용량(50MB)을 초과하니, 더 큰 파일을 추가할 때는 Git LFS 사용을 먼저 검토하세요.
- **버전 파일명 규칙**: 같은 주제의 파일이 `ver.1`, `v2`, `v3`, `_수정` 등으로 여러 버전 존재합니다. 새 버전을 만들 때는 기존 파일을 덮어쓰지 말고 새 버전 접미사를 붙여 저장하세요(과거 버전 보존이 이 프로젝트의 관례입니다).
- **xlsx 파일 직접 편집**: 엑셀 파일 내용을 읽거나 수정할 때는 xlsx 스킬을 사용하세요.
- **`.claude/settings.local.json`**: 개인 로컬 설정이므로 `.gitignore`에 포함되어 있습니다. 커밋 대상에 포함시키지 마세요.
- **한글 파일명**: 다수의 파일명이 한글입니다. 셸 명령에서 다룰 때 따옴표 처리에 주의하세요.

## 커밋 관례

- 이 저장소는 별도 빌드/테스트 파이프라인이 없는 데이터 전용 저장소입니다. 커밋 전 별도 빌드 검증은 필요 없으며, 변경된 파일이 의도한 파일인지 확인하는 것으로 충분합니다.
- 커밋 메시지는 무엇이 바뀌었는지(어떤 자료가 추가/갱신/정리되었는지) 한두 문장으로 명확히 남기세요.
